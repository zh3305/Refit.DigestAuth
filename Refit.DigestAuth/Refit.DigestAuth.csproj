<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>netstandard2.1</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
        <Title>Refit.DigestAuth</Title>
        <RepositoryUrl>https://github.com/zh3305/Refit.DigestAuth</RepositoryUrl>
        <Description>Digest Auth handler for adding Auth headers to outbound requests</Description>
        <PackageProjectUrl>https://github.com/zh3305/Refit.DigestAuth</PackageProjectUrl>
        <PackageId>RefitDigestAuth</PackageId>
        <LangVersion>latest</LangVersion>
	    <PackageVersion>1.0.1</PackageVersion>
    </PropertyGroup>

</Project>
